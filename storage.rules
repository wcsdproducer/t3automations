rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Development rule: Allows any authenticated user to read and write any file.
    // WARNING: This is insecure and should not be used in production.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
